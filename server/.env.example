# Example env file for server

# HMAC secret for signing QR tokens
SECRET=replace_with_a_secure_random_value

# Admin setup password (used for initial admin creation)
ADMIN_PASSWORD=change_this

PORT=4000
DB_FILE=./server/data.sqlite
UPLOAD_DIR=./server/uploads
ALLOWED_ORIGINS=*
